import SiteLayout from '@/appPages/site/layouts/SiteLayout'
import React from 'react'

const Layout = ({ children }: IChildren) => {
	return <SiteLayout>{children}</SiteLayout>
}

export default Layout
